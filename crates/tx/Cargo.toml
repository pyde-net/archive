[package]
name = "pyde-tx"
version = "0.1.0"
edition = "2021"

[dependencies]
pyde-crypto = { path = "../crypto" }
pyde-account = { path = "../account" }
pyde-vm = { path = "../pvm" }
pyde-state = { path = "../state" }
sparse-merkle-tree = "0.6"
tracing = "0.1"
hex = "0.4"
